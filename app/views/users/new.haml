#register
  - remote_form_for :user, :url => users_path do |f|
    %p
      %label 用户名
      = f.text_field :login
    %p
      %label 密码
      = f.password_field 'password'
    %p
      = submit_tag '注册', :class => "button"
