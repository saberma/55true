if @answered_question
  page.insert_html :top, 'all', :partial => 'home/answered_question.haml', :locals => {:answered_question => @answered_question}
  page.remove "q#{params[:last_id]}"
  page["q#{@answered_question.id}"].visual_effect :highlight, :startcolor => "#e5ecf9", :endcolor => "#f5f5f5"
end
