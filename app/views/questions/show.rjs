if @question_list.first
  page << "j('#all li:last').remove();"
  page.insert_html :top, 'all', :partial => 'home/answered_question.haml', :collection => @question_list
  page << "j('#all li:first').hide().show(500);"
end
