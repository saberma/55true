#get_question
  -form_for :question, :url => questions_path do |f|
    %p 接不到题，你先提问
    %p
      =image_tag(current_user.head, :class => "head") 
      = f.text_area :content, :rows => 2, :is_focus => true
    %p
      = submit_tag '提交', :class => "button"
