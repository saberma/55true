#get_question
  -remote_form_for :question, :url => questions_path do |f|
    %p 接不到题，你先提问
    %p
      =image_tag(current_user.head, :class => "head") 
      = f.text_area :content, :rows => 2, :is_focus => true
      %span.quiet="这里接题的顺序是随机的，您还可接题#{MAX_QUESTIONS - current_user.unanswered_questions.size}次"
      .clearfix
      %span.tip.quiet 请遵守游戏规则，无效问题、回答将被屏蔽
    %p
      = submit_tag '提交', :class => "button"
