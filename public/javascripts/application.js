//var socket = new io.Socket(null, {port: 3000});
//socket.connect();
//socket.on('connect', function(){ alert('connect'); });
//socket.on('message', function(msg){ $('#clients').text(msg['clients']); });
//socket.on('disconnect', function(){ alert('disconnect'); });
//socket.on('reconnect', function(){ alert('reconnected')});
//socket.on('reconnecting', function( nextRetry ){ alert('Attempting to re-connect to the server, next attempt in ' + nextRetry + 'ms')});
//socket.on('reconnect_failed', function(){ alert('Reconnected to server FAILED.')});
